using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RedirectManager
{
    //public Vector3 CalculateTranslationGain(Vector3 prevPos, Vector3 currPos)
    //{
    //    if (!float.IsNaN(prevPos.x))
    //    {
    //        deltaPos = currPos - prevPos;
    //        deltaPos = Utilities.ForwardDir3D(deltaPos);
    //        deltaPos = deltaPos * transGainRate;
    //        transform.position += deltaPos;
    //    }

    //}

    //public Vector3 CalculateCurvatureGain(Vector3 prevPos, Vector3 currPos)
    //{

    //}
}
